services:
  - type: web
    name: bank-fraud-detection
    runtime: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: cd dashboard/backend && uvicorn api_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
