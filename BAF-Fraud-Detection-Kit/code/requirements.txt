# Verified working on Python 3.14 (Windows) as of this kit's build.
# These exact versions were installed and the pipeline was run end-to-end.
lightgbm>=4.7.0
pandas>=2.2
scikit-learn>=1.5
numpy>=1.26
pyarrow>=15.0        # fast parquet; BAF ships parquet ~3x smaller than csv

# For the differentiators (install when you reach hour 10)
matplotlib>=3.8
shap>=0.45           # explainability / reason codes

# Optional
imbalanced-learn>=0.12   # only if you want SMOTE in the ablation
optuna>=3.6              # hyperparameter search
xgboost>=2.1             # for the blend
catboost>=1.2            # strong on anonymised categoricals
