# Requirements for the API layer only (fraud-detection/api/).
# Install alongside the root fraud-detection/requirements.txt (the ML
# pipeline's own deps: pandas, scikit-learn, lightgbm, xgboost, shap, etc.)
# since api/model_service.py imports directly from src/.
#
#   pip install -r ../requirements.txt
#   pip install -r requirements.txt

fastapi>=0.136,<0.137
uvicorn[standard]>=0.46,<0.47
pydantic[email]>=2.13,<3.0
pydantic-settings>=2.14,<3.0
SQLAlchemy>=2.0,<3.0
PyJWT>=2.13,<3.0
passlib>=1.7,<2.0
argon2-cffi>=23.1,<26.0
pyotp>=2.9,<3.0
cryptography>=42.0,<49.0
slowapi>=0.1.9,<0.2.0
python-multipart>=0.0.30
httpx>=0.28,<0.29
pytest>=8.0

# Not installed by default -- SQLite (stdlib sqlite3, no extra dependency)
# is the local-dev/demo target. Uncomment for a real Postgres deployment
# (see docker-compose.yml, which assumes this driver):
# psycopg[binary]>=3.2,<4.0
